(ns nlp-utils.stanford-attr-test
    (:use nlp-utils.stanford-attr)
    (:use nlp-utils.stanford-corenlp)
    (:use clojure.test))


(comment
(deftest attr-in-rel-test 
  (testing "Should return a monetary amount which is part of an 'of' preposition relationship"
)))
